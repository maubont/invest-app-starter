
services:
  web:
    build: ../apps/web
    ports: ["3000:3000"]
    environment:
      - NEXT_PUBLIC_ENGINE_URL=http://localhost:8000
    depends_on: [engine]
  engine:
    build: ../services/engine
    ports: ["8000:8000"]
